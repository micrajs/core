declare global {
  /**
   * The Application is a configurable and extensible namespace that can be used to define application-wide interfaces and functions.
   * This allows modules to be written in a modular fashion, to be easily extensible and maintain type safety.
   */
  namespace Application {}
}

export {};
